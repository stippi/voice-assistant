{
  "name": "voice-assistant",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "concurrently \"yarn workspace frontend dev\" \"yarn workspace backend start\"",
    "build": "yarn workspace shared build && yarn workspace frontend build && yarn workspace backend build",
    "test": "yarn workspace frontend test && yarn workspace backend test"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "prettier": "^3.3.3",
    "typescript": "^5.0.2"
  }
}
